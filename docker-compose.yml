version: "3.7"

services:
  wordpress:
    build: ./wordpress
    ports:
      - 8100:80
    environment:
      WORDPRESS_DB_HOST: db
      WORDPRESS_DB_USER: &dbuser wp_user
      WORDPRESS_DB_PASSWORD: &dbpassword wp_password
      WORDPRESS_DB_NAME: &dbname wp_db
    volumes:
      - wp-content:/var/www/html/wp-content
    depends_on:
      - db

  db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: *dbname
      MYSQL_USER: *dbuser
      MYSQL_PASSWORD: *dbpassword
      MYSQL_RANDOM_ROOT_PASSWORD: "1"
    volumes:
      - db-data:/var/lib/mysql

volumes:
  wp-content:
  db-data:
